
image_xscale = 0.25;
image_yscale = 0.25;

movement_speed = 18;
target_x = 0;
target_y = 0;

// Anything compatible with drawCard()
card = new DeckIcon();

// Must be an object with a nullary call() method, if supplied.
callback = new NullCallable();
